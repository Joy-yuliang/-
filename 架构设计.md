# 个人生活体系软件 · 架构设计（v0.1 草案）

> 依据《需求文档.md》编写。开发环境：Windows 11（联想笔记本），工作目录 `C:\Users\lenovo\体系`。
> 状态：草案，待用户确认后开工。

---

## 1. 技术选型

### 1.1 环境事实（本机实测）
- Node.js v24.19.0 + npm 11.17.0 ✅ 可用
- Rust / cargo ❌ 未安装（排除 Tauri，除非先装 Rust 工具链）
- git ✅ 已可用（`C:\Users\lenovo\体系\.tools\mingit` 便携版 git 2.55.0；项目已托管 GitHub `Joy-yuliang/-`，推送需走本地代理 127.0.0.1:7890 且 `http.sslBackend=openssl`）
- Python ❌ 仅商店占位（排除 Python 桌面方案）

### 1.2 选型结论：Electron + React + TypeScript + SQLite + DeepSeek API

| 方案 | 结论 |
|---|---|
| **Electron + React** | ✅ **推荐**：Node 现成；Web 技术做中文界面快、好迭代；通知/托盘/开机自启/文件读写/打包 exe 生态成熟；docx 读写有现成库 |
| Tauri + React | 需先安装 Rust 工具链（大安装），暂缓；未来可迁移 |
| Python + PySide6 | Python 不可用，放弃 |
| 本地 Web + 浏览器 | 用户已选独立桌面应用，放弃 |

- 数据库：**MVP 用 JSON 单文件存储**（零原生依赖、稳妥可靠、天然可备份）；V1 评估是否升级 SQLite（better-sqlite3 需匹配 Electron ABI，作为后续优化项）。
- 文档处理：**mammoth**（读 .docx）、**docx**（写/导出 .docx，WPS 可打开）。
- AI：**DeepSeek API**（OpenAI 兼容格式），API Key 存本地设置，可替换为其他兼容服务。
- 打包：**electron-builder** 产出 Windows 安装包（exe）。
- 代价说明：安装包约 100MB、内存占用略高——个人电脑完全可接受，换来的是开发迭代速度。

---

## 2. 总体架构（三层 + 外接）

```
┌─────────────────────────────────────────────┐
│ 界面层（React 渲染进程）                     │
│ 每日页 · 体系树 · 日记 · 读书 · 复盘 ·      │
│ 收集箱 · 导入导出 · 设置                     │
├─────────────────────────────────────────────┤
│ 主进程（Electron main）                      │
│ 文件/数据库访问 · 系统通知提醒 · 托盘 ·      │
│ 开机自启 · 导出文件 · AI 调用（Key 本地存）  │
├─────────────────────────────────────────────┤
│ 数据层：SQLite（data.db）+ 可选 Markdown 备份│
└───────────────┬─────────────────────────────┘
        ┌───────┴────────┐
        │ DeepSeek API   │  分析/提炼/复盘
        │ WPS 文件       │  导入 .docx / 导出 .docx
        └────────────────┘
```

原则：**数据纯本地、格式开放、一切结构可改、可导出备份**。

---

## 3. 数据模型（核心表草案）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `nodes` | 体系节点（树） | id, parent_id, name, type(知识/SOP/卡片), content, sort |
| `tags` / `node_tags` | 标签与节点关联 | 实现体系间交叉关联 |
| `columns` | 每日页栏目（可扩展） | id, name, sort, is_routine（是否固定例程） |
| `daily_plans` | 每日页 | date, column 快照 |
| `items` | 条目 | id, date, column_id, text, status(未做/完成/跳过), reason(未完成原因), due_time, start_time, remind_before_minutes, priority, duration_minutes |
| `journal_entries` | 日记（小体系） | id, date, content, mood, tags, linked_node_id |
| `reading_records` | 读书记录 | id, date, book, content(读了什么), feeling(感受), minutes |
| `inbox_items` | 收集箱 | id, created_at, text, status, target |
| `reviews` | 复盘 | id, period(日/周/月), date, content, stats_json |
| `settings` | 设置 | 提醒时间、API Key、SOP 配置等 |
| `logs`（顶层数组） | 操作日志 | `[{id, timestamp(ISO), category(plan/diary/system), message}]`，只增不减，日期分隔展示 |

**当前实际数据模型（tixi-data.json 单文件）**：`settings` / `columns`（栏目：安排/读书/临时起意/总结） / `routines`（8 步 SOP） / `systems[]`（体系树：id, parentId, title, content, createdAt, updatedAt） / `events{日期:[日程]}` / `days{日期:{routine,lists,reading,summary,tomorrow,diary}}` / `logs[]`（v0.8 新增）。

**关键机制**：SOP 型节点（`nodes.type='SOP'`）→ 每晚/每天自动生成次日固定例程条目到 `items`。

---

## 4. 页面与交互

1. **每日页**（核心）：顶部日期 + 进度；SOP 固定例程区（自动生成、逐项打勾）；栏目分区（安排/读书/临时起意/总结，可扩展）；「临时起意」按钮 → 记入收集箱或今日/明日；晚间检验区（未完成 → 追问原因）；次日计划区。
2. **体系页**：树状目录 + 标签过滤；节点详情（知识卡片 / SOP 步骤 / 关联的日记与记录）；修改 SOP 节点 → 次日执行联动。
3. **日记页**：时间线（历史回看、成长轨迹）+ 编辑器 + 标签；条目可一键转收集箱/体系卡片；支持导入 .docx / 粘贴文本。
4. **读书页**：在读清单 + 每日记录栏（内容 + 感受 + 时长）。
5. **复盘页**：日/周/月；完成率统计 + 未完成清单 + AI 总结草稿 + 引导式模板。
6. **收集箱**：待整理列表 → 一键转明日计划 / 体系卡片 / 日记。
7. **导入导出**：拖入 .docx / 粘贴 → AI 提炼预览 → 归档到体系；导出当天安排/日记/复盘为 .docx（WPS 打开）。
8. **设置**：提醒时间、API Key、开机自启、数据备份位置。

---

## 5. AI 集成设计

- 调用 DeepSeek API（OpenAI 兼容），Key 存本地设置文件，可换服务。
- 首版功能：
  1. **导入提炼**：导入的日记/记录 → 自动归纳要点、建议归入哪个体系节点；
  2. **灵感/日记归类**：收集箱内容 → 生成体系卡片草稿；
  3. **周/月复盘总结**：读本周记录自动生成总结草稿供用户修改。
- 后续：自然语言搜索（如「我以前写过关于 XX 的什么」）。
- 隐私：仅把用户选中的文本发送给 API；可全局开关 AI。

---

## 6. 开发路线图

> 用户已确认：**先做「生活板块」（MVP），边用边改**；学习、创作等体系在体系树上逐步长出。

| 阶段 | 内容 | 交付 |
|---|---|---|
| **MVP（生活板块）** | 应用骨架 + 每日页（SOP 轨道 8 步自动生成 + 安排/读书/临时起意/总结栏目 + 打勾 + 未完成原因 + 次日计划）+ 临时起意一键转明日 + JSON 本地存储 + 设置（SOP 编辑/栏目管理）+ 导出当天安排 docx + 23:00/24:00 例程提醒 | 可运行 exe |
| **V1** | 体系树（含 SOP 型节点联动、学习/创作体系挂入）+ 日记（编辑器/时间线/导入）+ 读书记录 + 收集箱 | 可运行 exe |
| **V2** | AI 提炼/复盘 + 任务级提醒 + 周/月复盘页 + 历史批量导入 + SQLite 评估 | 可运行 exe |
| **V3+** | 自然语言搜索、界面打磨、按用户反馈持续迭代 | 持续更新 |

每阶段完成即交付可双击运行的安装包，用户边用边提需求，继续迭代。

---

## 7. 开发目录规划

```
C:\Users\lenovo\体系\
├── 需求文档.md          ← 活文档，随交流更新
├── 架构设计.md          ← 本文档
├── 使用说明.md          ← 用户操作手册
├── 启动个人体系.bat     ← 一键启动
└── app\                 ← 软件项目（Electron + React）
    ├── src\             （React 界面 + 主进程逻辑）
    ├── data\            （运行时生成）
    ├── dist\renderer\   （构建产物）
    └── node_modules\    （依赖）
```

---

## 8. 当前进度（2026-08-18/19 实测）

- **v0.1 生活板块 MVP 已构建并验证通过**：
  - 依赖安装完成（npm 11 的 allow-scripts 机制需手动执行 electron/esbuild 安装脚本）；
  - `vite build` 成功（34 模块，无错误）；
  - Electron 冒烟测试：启动 10 秒存活、无 stderr 报错；
  - 本地数据文件正常生成：`%APPDATA%\个人体系\data\tixi-data.json`（UTF-8，含默认设置/栏目/8 步 SOP）。
- **v0.2 日记页已构建并验证通过**：
  - 新增 `lunar-javascript` 依赖（农历/节日/节气，实测劳动节、春节显示正确）；
  - 新增日记页：日历（翻月翻年、农历标注、日记圆点）、音乐/天气、事件/思考/情绪/随笔、导出日记 docx；
  - `vite build` 成功（40 模块）；Electron 冒烟测试：启动 10 秒存活（沙箱环境下的 Chromium cache 报错为测试环境产物，用户正常运行时无影响）。
- **v0.2 已打包为便携 exe 并验证**：
  - `electron-builder` 打包成功（`signAndEditExecutable:false` 跳过签名/exe 编辑，规避 winCodeSign 符号链接权限问题）；
  - 产物：`app\release\个人体系.exe`（71.7 MB 单文件便携版，双击即用）；`app\release\win-unpacked\个人体系.exe`（未压缩版）；
  - 打包版启动测试通过（10 秒存活无崩溃）。
- **v0.3 体系页已构建、打包并已运行**：
  - 新增 `marked` + `dompurify` 依赖（Markdown 渲染 + 消毒）；
  - 体系页：便签式多级体系（任意深度递归，含标题/字数/更新时间）、Markdown 编辑与预览、体系树视图（点击节点查看内容）、移动/级联删除；
  - `vite build` 成功（43 模块）；冒烟测试通过；已重新打包便携 exe 并替换工作区副本（71.9 MB），新版已启动运行。
- **v0.5 计划/体系/日记交互全面打磨（按用户反馈逐轮调整）**：
  - 「每日」改名「计划」；「便签视图」改名「体系建构」；「体系树」改名「总体预览」；
  - 卡片整行点击展开/收起；折叠态点击「编辑/移动」自动展开；输入框高度加倍；总结/次日计划/日记四分区接入右下角 ⤢ 放大图标 + 大编辑框（编辑/预览 + 撤销/恢复）；
  - 日记页移除大日历改为顶部日期点击；吸顶无边框栏（时间/音乐/天气）；「复制→明天」保留当日记录。
- **v0.7 全局搜索上线**：
  - 顶栏 🔍 全局搜索：扫描计划/日记/体系全部文本，整句高亮片段，点结果跳转并高亮（textarea 自动选中搜索词）；
  - 搜索结果标签统一 2 字（计划/日记/体系）+ 副标签（日期 · 类型），标签宽度贴合文字、片段自动换行不溢出（修复 Chromium flex 换行 bug）；
  - 跳转后浮动「← 返回搜索」按钮；各页面常驻挂载不丢编辑状态；搜索状态提升到 App 层。
- **v0.8 操作日志 + Toast + 全局换行 + 体系编辑增强（2026-08-21）**：
  - 新增「日志」页签（顶栏：计划 日记 体系 设置 日志 + 🔍）：全站计划/日记/体系有效操作（增删改/完成/未完成/复制到明天/导出/日程等）自动记录 `logs[]` 并弹 Toast（顶部居中、2 秒淡出、垂直堆叠、不区分颜色）；
  - 日志页：正序排列最新在底部、自动滚动到最新；年份大字标题（28px）+ 日期「M月D日 星期X」标题（18px）；首条记录显示年份；跨年插入新年份标题；分类标签 计划蓝/日记绿/体系橙；
  - 体系卡片时间改为「创建 HH:mm · 更新 HH:mm」（具体到分钟，按本机时区）；
  - 体系编辑框接入右下角 ⤢ 放大图标 + 大编辑框（编辑/预览 + 撤销/恢复，撤销历史每次打开重置）；
  - 全局 CSS：禁止横向滚动、所有长文本（中文/英文/数字/链接）自动换行（`overflow-x:hidden` + `overflow-wrap:break-word`）；
  - 自动验证 26/26 通过；已打包 `release\个人体系.exe`（package.json version 0.8.0）。
- **v0.9 日志页增强 + 体系预览增强（2026-08-21）**：
  - 日志页：月份分组大字标题「YYYY年M月」（28px）；导出日志 docx（月份/日期分隔 + 彩色分类 + 完整消息）；单条删除（行内确认）+ 月份批量删除（复选框/全选/确认删除/取消，全选红色确认）；「最新日志」按钮平滑滚动到底部 + 高亮闪烁；删除不新增日志；
  - 修复日志标签「日记」占满整行 bug（全局裸类 `.diary` 与 `.logtag.diary` 冲突 → `.logtag` 加 `width:auto` 等覆盖）；
  - 总体预览「全部展开 / 全部收起」（forceOpen 派生 + manualOpen 覆盖；修复子节点漏传 forceOpen 导致只展开一层的 bug）；
  - 日记「情绪」改名「感受」（界面/搜索/导出同步，数据键 mood 保留）；
  - 自动验证 19/19 + 导出 docx 内容验证通过；package.json version 0.9.0；已打包运行。
- **启动方式（用户）**：桌面快捷方式 `个人体系` 或 `C:\Users\lenovo\体系\个人体系.exe`；开发/调试用 `启动个人体系.bat`。
- **待办**：应用图标、读书记录强化、AI 提炼、周/月复盘、任务级提醒。
- **开发注意**：本机 npm 需用 `npm.cmd`（PowerShell 执行策略禁止 .ps1）；数据文件用 UTF-8；依赖安装与构建需完整权限（沙箱限制）。
