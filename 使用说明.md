# 个人体系软件 · 使用说明（v0.9：计划 + 日记 + 体系 + 日程 + 全局搜索 + 操作日志）

## 如何启动

**最快方式（已为你设置好）：**
- **桌面快捷方式**：双击桌面的 `个人体系` 图标；
- 或者双击 `C:\Users\lenovo\体系\个人体系.exe`（工作区根目录的独立副本）。

备用方式：双击 `app\release\个人体系.exe`；开发/调试用 `启动个人体系.bat`（需要 Node 环境）。

## 计划页（核心页面）

顶部：日期切换（‹ ›）、回到今天、今日完成进度条、「导出安排」按钮。

### 例行公事（每日 SOP 轨道，8 步）
- 每天自动出现，做完点 ✓，没做完点 ✗ 并**如实记录原因**（第二天纠正）；
- 步骤可在「设置 → 每日 SOP 步骤」里增删改、排序，第二天自动生效。

### 四个默认栏目
| 栏目 | 用法 |
|---|---|
| 安排 | 今天的计划，逐项打勾；**点条目文字可随时修改（改错字不用删除重来）**；未完成点 ✗ 记原因 |
| 读书 | 读完写：内容 + 感受 + 时长（目标 ≥ 1 小时） |
| 临时起意 | 随手记想法；小事先做，做不完点「**复制→明天**」——**复制**到明天安排（今天的记录保留，因为计划也是日记的一部分） |
| 总结 | 写日记、写感悟、做总结反思（SOP 第 6 步） |

底部「次日计划」：今晚规划明天，让第二天无需纠结。

## 设置页

- **每日 SOP 步骤**：增删改 8 步轨道；
- **栏目管理**：可新增自己的栏目（清单型）、改名、删除自定义栏目；
- **提醒**：23:00 开始总结、24:00 睡觉（可开关、可改时间，软件开着时到点弹系统通知）。

## 日记页（v0.2 新增，v0.5/0.6 改版）

- **布局**：上方是**吸顶栏**（不加框，滚动时一直固定）：**时间（可点击弹日历）+ 今日音乐 + 天气**；下方**事件、感受、思考、随笔各自独立成卡片**（v0.9 起「情绪」改名「感受」）；
- 选择今天以外的日期时，吸顶栏会出现**「回到今天」**按钮；
- 每个写字框较高、**右下角 ⤢ 图标**：点开**接近全屏的大编辑框**（可容纳大量文字），顶部有 **编辑 / 预览** 页签 + **⟲ 撤销 / ⟳ 恢复** 按钮；
- 「导出日记」→ 生成 Word 文档（.docx），WPS 可直接打开编辑。

> 计划页的「总结」「次日计划」文字框与日记一致：右下角 ⤢ 展开 + 编辑/预览 + 撤销/恢复。

## 体系页（v0.3 新增）⭐ 核心功能

顶部「体系建构 / 总体预览」切换。

### 体系建构（便签式，像便签/备忘录一条一条）
- **「＋ 新增一级体系」**：点击展开编辑区（标题 + 内容，输入框大且舒适），保存后成为一张卡片；
- 每张卡片显示：**层级（一级/二级/三级…）、标题、字数、创建/更新时间（精确到分钟，如 创建 14:35 · 更新 15:02）、子体系数量**；**点击整条任意位置即可展开 / 收起**（比左右箭头更顺手）；
- **编辑 / 移动 / 删除**：点按钮会自动展开卡片并弹出对应界面；**切换到别的页签再回来，正在编辑的内容不丢失**；
- 编辑区文字框**右下角 ⤢ 图标**：点开**大编辑框**（编辑 / 预览 + ⟲ 撤销 / ⟳ 恢复，撤销历史每次打开重新开始）；
- 展开后点 **「＋ 新增二级体系」**，二级里再点可加**三级**……**任意深度（可到四级、五级…十六级）**，例如：学习 → 英语学习 → 四级怎么学 → 四级单词 / 四级短语（内容写具体实行的笔记、记录、心得）；
- 内容**支持 Markdown**（编辑 / 预览切换）：`# 标题`、`- 列表`、`**加粗**`、`> 引用` 等。

### 总体预览（树状图，可切换其他图式在后续版本）
- 点「总体预览」→ 从"体系"延伸出树状图；**点开一级体系 → 下面展开二级体系，点开二级 → 展开三级**，逐级展开；
- 顶部有 **「全部展开 / 全部收起」按钮**：一键递归展开 / 收起所有层级（不影响手动逐级点开收起）；
- 点节点 → 右侧显示该体系的内容（查看，编辑请回体系建构）。

## 数据与导出

- 数据保存在本机：`C:\Users\lenovo\AppData\Roaming\个人体系\data\tixi-data.json`（纯本地，备份这个文件即可）；
- 「导出安排」「导出日记」→ 生成 Word 文档（.docx），WPS 可直接打开编辑。

## 日程与全局搜索（v0.4 新增）

### 时间可点选
- **计划页**：顶部日期可点击 → 弹出小日历选日期；
- **日记页**：日期标题可点击 → 弹出小日历选日期；
- 方便提前安排和回顾。

### 日程日历（计划页「📅 日历」按钮）
- 弹出**大日历框**：« ‹ › » 翻年翻月，显示农历/节日；
- 点某一天 → 右侧显示当天日程；点 **「＋ 添加日程」** 设置：**日程名称、开始时间、结束时间、地点、备注**；
- 有日程的天，格子下方显示**红色「有日」**；点开可查看具体日程（名称、起止、地点、备注），可删除；
- 提醒系统后续版本接入。

### 全局搜索（顶栏 🔍 放大镜）
- 点 🔍 → 下方弹出搜索框；输入文字即搜索 **计划 / 日记 / 体系** 全部内容；
- 每条结果 = **前面一个小标签（统一两字：计划=蓝、日记=绿、体系=橙，宽度贴合文字）** + 日期/体系名（如 `2026-08-19 · 总结`）+ **包含搜索词的完整一句**（命中文字黄色高亮，最多两行）；
- **点击一条结果 → 跳到那一页的那一句话**（计划/日记自动切日期并定位，文字框会直接选中搜索词；体系自动展开到目标层级），右下角出现**临时「← 返回搜索」**；
- **点击搜索框和结果列表之外的地方** → 自动转到搜索到的那一页（最近点过的结果；没点过则跳第一条），搜索框收起，「返回搜索」键消失；
- 再点 🔍 打开，搜索词和结果仍在，可继续用。

## 操作日志（v0.8 新增，v0.9 增强，顶栏「日志」页签）

- **顶栏「日志」**（搜索框左边）→ 打开操作日志页：自动记录你在**计划 / 日记 / 体系**页做的每一次有效操作（添加、编辑、删除、完成、标记未完成、复制→明天、导出、日程增删、体系增删移等），**只增不减**；
- 每条日志：**时间 HH:mm + 分类标签（计划=蓝、日记=绿、体系=橙）+ 完整消息**（如「您添加了安排：背单词 100 个」）；
- **排列**：按时间正序（早→晚），**最新在底部**，打开页面自动滚到最新；
- **月份分组**：大字标题按「**YYYY年M月**」（如「2026年8月」，28px）分组；同月内日期变化时显示小字「M月D日 星期X」（如「8月21日 星期五」）；换月 / 跨年自然出现新的大字标题；
- **标题行按钮**（右上角）：
  - **最新日志**：平滑滚动到底部并**高亮最新一条**（黄底闪烁约 2 秒）；日志为空时按钮置灰；
  - **导出日志**：把全部日志导出为 Word 文档（.docx，WPS 可打开，含月份/日期分隔、时间/分类/完整消息），导出后提示「您导出了日志」；
- **删除日志**：
  - **单条删除**：每条日志右侧 🗑 → 确认「您确定要删除该日志吗？」→ 确定后删除，提示「您删除了一条日志」；
  - **批量删除**：每个月份大字标题行右侧有「批量删除」按钮 → 进入该月份批量模式：该月每条日志左侧出现复选框 + 标题行出现「全选 / 确认删除 / 取消」；只可勾选**当前月份**；点「确认删除」弹确认（勾选 2 条及以上但非全选 → 「您确定要删除这些日志吗？」；全选 → **红色**「您确定要全部删除吗？」），确定后提示「您删除了 N 条日志」；
  - 删除操作**本身不新增日志**（避免日志无限增长）；
- **Toast 提示**：每次操作后，当前页面**顶部居中弹出黑色小提示**（内容与日志相同），显示 2 秒淡出；连续操作会**垂直堆叠**显示，互不覆盖；
- 长内容（条目文字 / 标题）超过 20 字时自动截断加「…」；
- 设置页的操作暂不记录；取消勾选（取消完成）不记录。

## 长文本自动换行（v0.8）

- 全应用所有页面 / 弹窗 / 编辑框**禁止横向滚动条**：中英文、数字、链接等长文本一律自动换行，不会把页面撑出横向滚动。

## 版本计划

- v0.1：计划页 + SOP + 栏目 + 临时起意 + 导出 + 提醒
- v0.2：+ 日记页（日历翻月翻年、农历/节日、音乐/天气、事件/思考/情绪/随笔、导出）
- v0.3：+ 体系页（体系建构便签式多级体系 + Markdown + 总体预览树状图 + 移动/删除）
- v0.4：+ 时间可点选日历、日程日历（名称/起止/地点/备注、红色「有日」）、全局搜索（🔍 跳转 + 临时返回键）、清单条目点文字修改、临时起意复制到明天
- v0.5：日记页改版（去大日历、更宽敞、展开图标在写字框右下角、展开框加大 + 撤销/恢复）、计划页文字框同步升级、修复体系搜索跳转空白与结果条目显示
- v0.6：标签页切换常驻不丢编辑状态、细节打磨
- v0.7：全局搜索标签统一（计划/日记/体系两字标签 + 日期·类型副标签）、标签宽度与片段换行修复
- v0.8：+ 操作日志页 + Toast 提示、体系编辑框放大 + 撤销/恢复、体系时间精确到分钟、全应用长文本自动换行
- v0.9（当前）：日志页增强（月份分组大字标题、单条/批量删除、导出日志 docx、最新日志滚动高亮）、修复日记标签占满整行、总体预览全部展开/收起、日记「情绪」改名「感受」
- 后续：读书记录强化、AI 提炼、周/月复盘、任务级提醒、应用图标
